## 2024-05-24 - [Promise.all for platform metrics]
**Learning:** Sequential database count queries can be optimized by executing them concurrently using Promise.all, which reduces overall database latency.
**Action:** Use Promise.all() to run independent db count queries concurrently.
